# Supabase alarm-push 설정 — 이 파일을 복사해 .env.alarm-push.local 로 저장 (git 제외)
# cp alarm-push.env.example .env.alarm-push.local
# VAPID 생성: bunx web-push generate-vapid-keys

SUPABASE_PROJECT_REF=

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:you@example.com
ALARM_CRON_SECRET=
